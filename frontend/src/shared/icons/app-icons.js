import { FontAwesome6 } from '@expo/vector-icons';

export function HomeIcon({ color, size = 20 }) {
  return <FontAwesome6 name="house" size={size} color={color} />;
}

export function ItemsIcon({ color, size = 20 }) {
  return <FontAwesome6 name="shirt" size={size} color={color} />;
}

export function AccountIcon({ color, size = 20 }) {
  return <FontAwesome6 name="user" size={size} color={color} />;
}

export function SunIcon({ color, size = 24 }) {
  return <FontAwesome6 name="sun" size={size} color={color} />;
}

export function SparklesIcon({ color, size = 20 }) {
  return <FontAwesome6 name="wand-magic-sparkles" size={size} color={color} />;
}

export function ShirtIcon({ color, size = 20 }) {
  return <FontAwesome6 name="shirt" size={size} color={color} />;
}

export function LayeredShirtIcon({ color, size = 20 }) {
  return <FontAwesome6 name="shirt" size={size} color={color} />;
}

export function HangerIcon({ color, size = 20 }) {
  return <FontAwesome6 name="shirt" size={size} color={color} />;
}

export function ChevronRightIcon({ color, size = 16 }) {
  return <FontAwesome6 name="chevron-right" size={size} color={color} />;
}
